# Configuração do dataset para YOLO
path: data/processed

train: images/train
val:   images/val
test:  images/test

# Número de classes — ajuste conforme seu projeto so placa no caso 
nc: 1

# Nomes das classes — ajuste conforme seu projeto placa no caso 
names: ['placa']
